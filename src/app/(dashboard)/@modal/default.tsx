/**
 * Slot padrão do parallel route @modal. Para toda rota que NÃO é `/t/[taskId]`
 * (ou em navegação dura/refresh), o slot fica vazio — a lista renderiza normal.
 */
export default function ModalDefault() {
  return null
}
